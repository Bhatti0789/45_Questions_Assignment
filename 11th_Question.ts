/* Names: Store the names of a few of your friends in a array called names. Print each person’s name by accessing each 
   element in the list, one at a time. */

let friendsNames = [ "Ayan" , "Tayyab" , "Sidra" , "Momal" , "Kinza" ]

    console.log ( friendsNames[0] + "\n" + friendsNames[1] + "\n" + friendsNames[2] )

// Distributed due to length of the code

    console.log ( friendsNames[3] + "\n" + friendsNames[4] )